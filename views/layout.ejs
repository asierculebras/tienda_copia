<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title> Tienda </title>
  <link rel="shortcut icon" href="favicon.ico" type="image/x-icon" />
  <link rel='stylesheet' href='/stylesheets/style.css' />
  <script src="qrcode.js"></script>
  <script type="text/javascript" src="jquery.min.js"></script>
<script type="text/javascript" src="qrcode.js"></script>


</head> 


<body>
<header class="main">


<div class="right">

<!-- la llamamos sesion por que esta definida en res.locals, y no hay que hacer lo de req.session, en cada una de las peticiones, lo almacenamos en local. -->
          <% if (!session.user) { %>
              <a href="/users/new">Registrarse</a> 
              <a href="/session">Login</a>
          <% } else { %>
              Hola <strong><%= session.user.username %> </strong> 
              <a href="/users/<%= session.user.id %>"> Perfil</a>
              <a href="/session?_method=DELETE"> Logout</a>
          <% } %>
        </div>


  <h2>Tu supermecado de confianza</h2>
  </header>
  <nav class="main" role="navigation">
        <a href="/">Inicio</a>
        <a href="/productos">Productos</a>
        <a href="/users">Usuarios</a>
        <% if ( session.user ) {%>
        <a href="/compra">Compra</a>
        <%}%>

      </nav>

      <section class="main">
        <div>

            <%- body %>

        </div>
  </section>
  </body>
</html>